# CLAUDE.md

## 全局硬规则（每次会话必须遵守）

1. **边界**：不修改授权范围之外的文件；新增 / 删除文件前先确认。
2. **证据**：任何"已完成 / 已通过"的声称必须附可验证证据（build/测试输出、`file:line`、`git diff`），禁止裸断言。
3. **决策归属**：关键架构 / 设计决策由用户拍板，只做分析 + 推荐 + 指出风险，不擅自替用户决定。
4. **验证门禁**：复杂任务走 subagent 团队（cto → red-team → tech-writer → developer → verifier，架构/代码后各做一轮对抗评审），verifier 通过才算完成，失败回环修复，不跳过验证直接交付。
5. **根治不打补丁**：修 bug 先证据先行、100% 定位根因再动手，禁止打补丁掩盖症状；修复要能解释"为什么这是根因"，并加防护测试防回归。
6. **设计先行、看整体**：动手前退一步看整体，不局限局部改动；先参考业内成熟方案与最佳实践，设计整体方案，再进代码。
